# 고정 이유: 카드·입력칸 CSS가 Streamlit 내부 선택자 의존 — Cloud 무단 업그레이드로 색감 원복 방지. 갱신은 로컬 검증 후 수동.
streamlit==1.58.0
pytest>=8.0
requests>=2.31
python-dotenv>=1.0
# 조견표(구글시트) 읽기 — 주문가공기와 동일 인증(서비스계정)
gspread>=6.1
google-auth>=2.29
# 체험단 양식 생성 — 빈 docx 템플릿 셀 채우기(python-docx)
python-docx>=1.1
# AI 키워드 자동완성 — Claude API(체험단 양식 Step4)
anthropic>=0.40
# 체험단 성과 분석 — 엑셀(xlsx) 다운로드
openpyxl>=3.1
